package process

// ContainerAddrClean contains only the fields explicitly defined in proto/process/agent.proto.
// Compared to the GO struct generated by `protoc`, it gets rid of the `protoimpl` private fields.
// This makes it suitable for use as key map.
type ContainerAddr struct {
	Ip       string
	Port     int32
	Protocol ConnectionType
}

func (c *ContainerAddrP) Clean() ContainerAddr {
	return ContainerAddr{
		Ip:       c.Ip,
		Port:     c.Port,
		Protocol: c.Protocol,
	}
}
